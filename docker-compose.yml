version: "3.8"
services:
  bot:
    build: .
    volumes:
      - ./projects:/app/projects
      - ./examples:/app/examples
      - ./templates:/app/templates
    environment:
      - PYTHONUNBUFFERED=1